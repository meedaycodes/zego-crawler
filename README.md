# domain-crawler

A take-home exercise: a concurrent, single-domain web crawler.

This branch is the starting point. The full implementation, test suite,
and write-up arrive via the **solution** pull request opened against
`main` — review there.
